function FormSetter(id, setID, serv, setServ, mail, setMail, name, setName, pass, setPass) {
    setID(id);
    setServ(serv);
    setMail(mail);
    setName(name);
    setPass(pass);
}

export default FormSetter